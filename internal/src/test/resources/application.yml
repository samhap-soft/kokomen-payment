spring:
  profiles:
    include:
      - domain-test
      - common-test
      - external-test
  main:
    lazy-initialization: true
  jackson:
    property-naming-strategy: SNAKE_CASE
    default-property-inclusion: non_null
